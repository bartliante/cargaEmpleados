/**
 * Placeholder service so the CAP server can boot and serve app/carga-empleados.
 * Real entities for the SFSF upload flow will be added once the backend
 * (persistence, validation against SFSF metadata, actual OData push) is designed.
 */
service CargaEmpleadosService {}
